# 2026-09-14 工作日志

## gh-pages 站点化：两份企业分析报告 + 全量摸底系列上线就绪

**用户要求**：①把分析报告做成**可在 gh-pages 呈现**的页面，版式同腾讯简报；②再选一个**高置信、最新**的广东企业出报告，同样上线。

**① 站点生成器 `scripts/make_site.py`（新增）**
- **只复用视觉与结构**：`from make_brief import CSS, page_head, thumb_img, NOTICE_HTML`
  （`make_brief.py` 的数据源是穿透流水线的 `CompanyReport`，与全量摸底的分册抽取结果模型不同，故不共用数据层）；
- 自研**极简 Markdown→HTML**（覆盖本项目用到的语法：`##`→card(`h2.bar`)、表格、列表、引用块、代码块、
  `---`、行内粗体/代码/链接）；页头补充区 = 第一个 `##` 之前的引用块（作声明条）；
- **Mermaid**：`<pre class="mermaid">` **保持原文不转义**（`-->` 若转义成 `--&gt;` 会导致渲染失败），
  通过 jsDelivr CDN 加载 mermaid@10；CDN 不可达时代码块仍可见（优雅降级）；
- 产出 `output/site/`：**13 个页面** + `icon.png` + `index.html`/`brief.html`（= 站点首页，
  分组导航：① 企业分析报告 ② 全量摸底口径/视图/证据卡 ③ 既有腾讯示例页），
  并复制 `tencent_deck.html` / `tencent_brief.html` 保留对照。

**② 高置信最新广东企业 = 錢大媽國際控股（id 108804）** → `output/红筹架构分析_錢大媽_108804.md`
选它而非 Exegenesis：**核心层**（命中「公司资料」章）+ 词频 **103**（广州×72）+ 递表 **2026-08-21**。
实抓事实（招股书分册）：
- 上市主体 Qdama International Holding Ltd.，**开曼群岛** 2021-05-12 注册（豁免公司，法定股本 USD 50,000）；
- **境内总部及主要营业地：广州市海珠区广州大道南 1601–1603 号 A 区广州创投小镇 69 号楼**（核心层证据）；
- 业务：中国最大**社区生鲜连锁**，加盟模式，2025 年社区生鲜占中国生鲜零售 10.2%，公司生鲜 GMV 份额 0.9%，
  七大参与者中唯一社区生鲜连锁品牌，触达消费者 3,100 万+；
- 创始人 **冯女士与冯先生（姐弟）** 2014 年创立**广州钱大妈**（集团主要运营实体）；员工持股平台广州钱掌柜（2016-09-26）；
- 重组 Step 1 于 2021-04 设立 BVI SPV **FJS Holding Ltd. / Feng WH**；子公司广州钱大妈（100%，2014-05-16，品牌管理）、
  广州钱大妈供应链（100%，2020-02-12）、东莞钱大妈农产品（100%，2016-10-12）；
- 冯女士经 FWH Holding Ltd./Feng WH/WH Feng Ltd./Qdama Talent 合计控制 **51.26%** 投票权；
- **无 VIE**（股权控制），vie_terminated=False。
报告结构对齐腾讯模板：路径审查 / 企业概况 / 穿透链条 / 历史沿革与重组 / VIE 判定 / 交叉验证 / 风险提示 / 架构图。

**本地预览**：`python -m http.server 8765 --bind 127.0.0.1`（在 `output/site/` 下），已 200 验证。

**⚠️ 待用户授权**：推送 gh-pages 属远程操作，**未执行**；需用户明确同意后再 `git push origin <site>:gh-pages`。

**渲染自检**：qdama 8 cards/3 表、exegenesis 9 cards/2 表、index 3 cards/3 表；Markdown 标记残留 0；mermaid 各 1。

---

## gh-pages 改版：index 导航模式 + 按日期批次目录 + 腾讯样例打标（**已上线**）

**用户要求**：①站点改成 index 模式；②新报告按**腾讯模版（风格、板块一致）**做；③传上去放**独立文件夹（按日期）**；
④index 上**企业名可点击**跳报告页；⑤腾讯**打标「样例」**。

**关键决策（用户拍板）**：板块 = **会前简报 4 板块**（① 一页画像 ② 触点地图 ③ 商机矩阵 ④ 干系人作战表）。
注：线上「腾讯」原有两套结构——`会前简报`（4 板块，业务导向）与 `穿透报告`（markdown 章节）。
用户选定会前简报；简报内容由 `make_brief.render_deck` 生成，但其 ② 触点地图**写死了穿透流水线的五行**
（境外股东层/离岸层/境内 WFOE/VIE 运营实体/个人层），照搬会产生**事实错误**（钱大妈无 VIE、Exegenesis 仅非法人团队）。

**实现**
- 新增 `scripts/brief_data.py`：两家企业的 4 板块**结构化数据**（事实全部取自招股书分册）；
- 重写 `scripts/make_site.py`：
  - `render_brief()` **数据驱动**渲染 4 板块，复用 `make_brief.CSS`（**与腾讯页逐字节相同**）；
    新增优先级色阶 `n`「待核验·需先落主体」（复用既有 `.tag.n` 样式）；
  - 站点布局：`index.html`（根导航）+ `<YYYY-MM-DD>/`（批次目录，含简报+详情+摸底 9 文档）+ `sample/`（腾讯样例）；
  - `_mark_sample()`：复制腾讯页时注入**页首横幅** + `<title>【样例】` 前缀；
  - 子目录各复制一份 `icon.png`（页内 `<img src="icon.png">` 是相对路径）；
  - 无 `##` 分节的文档（guangdong/unverified）整体包一张 `.card`（原先内容落在卡片外）；
  - 批次名默认 `dt.datetime.now(config.CST)`（**config 无 `now_cst()`**，全项目约定写法是 `dt.datetime.now(config_mod.CST)`）。
- 新增 `.nojekyll`。

**自检**：21 个文件、**0 断链**、0 Markdown 残留；`guangdong/unverified` 卡片包裹已修；
腾讯页骨架 250 元素 vs 新简报 255，**元素/class 词汇完全一致**，差异仅数据行数；**CSS 逐字节相同**。

**已推送（用户明确授权）**：`main` 工作树用 `git worktree add --detach /tmp/ghp-push gh-pages` → 清空旧页 → 复制
`output/site/` → commit `a742931` → `git push origin HEAD:gh-pages`（`a3691a1..a742931`）；随后
`worktree remove` + `fetch origin gh-pages:gh-pages` 同步本地引用。
**线上核验**：`https://shengc-shv.github.io/redchip-penetration/` 首页/批次页/简报页/样例页 全部 **200**，
首页企业名链接与「样例」打标均已生效。

**踩坑**：`git remote -v` 触发敏感信息审批（输出含凭据）被拦截 → 后续一律不打印远程地址，直接用 `origin` 引用。


---

## Exegenesis 复核修正（重大事实更正）+ index 极简化（**已上线**）

**用户指令**：「刚才杭州是主体那也要做」→ 经澄清选择「就是 Exegenesis（复核）」；随后追加
「index 只要第 1 部分，腾讯会前简报放第一条并打标（样例），index 尽量简单」。

### 一、Exegenesis 原报告结论**错误**，已更正
- 原判：「广东连接为**团队级、非法人级**」「重大子公司表未列广州法人主体」——**错**。
- 事实（招股书原文）：**OUR MAJOR SUBSIDIARIES** 表明列 **Guangzhou Jiayin（广州嘉因）**
  = PRC · **2025-11-11** 设立 · 公司持股 **100.00%** · 基因治疗研发；且「Reorganization」章明示
  「the **Guangzhou Jiayin became our principal PRC operating entity**」。
  重组三步：① 在**中国（广东）自由贸易试验区**设立广州嘉因 → ② 杭州嘉因将**全部 CGT 业务及资产**
  转让给广州嘉因（对价 **RMB122,944**）→ ③ **2025-11-25 签署 VIE 终止协议**、由 WFOE
  **杭州 Exegenesis** 收购杭州嘉因全部股权。另获**广东省药监局（GMPA）**书面确认。
- 我此前还漏列两个境内主体：**杭州 Exegenesis**（2019-08-01，100%，WFOE，收购杭州嘉因的实施主体）、
  以及南京隐领的真实持股口径（**54.00%** by Hangzhou Exegenesis，而非"由合伙企业 40% + 自然人 6% 持有"）。
- 新增事实：**万联广生**（万联证券全资子公司；万联证券 43.04% 由**广州金融控股集团**持有、
  广州市财政局控制）2025-12-26 与公司及**广州嘉因**签可转债，本金 **RMB 20,000,000**。
- 法定股本核实无误：**USD50,000.00 / 50,000 股 / 每股面值 USD1.00**。

### 二、根因与代码修正（**影响全量，不只这一家**）
| # | 问题 | 修正 |
|---|---|---|
| 1 | **分册页数预算过低**：`scan_operating_region` 对所有分册统一 `max_pages=10`，但「历史沿革」章实质内容在**百余页之后**（Exegenesis 的广州嘉因在第 114–133 页）→ 证据整段漏读，词频被低估为 5 | 新增 `_OPCO_SECTION_PAGES = {SUMMARY:12, CORPORATE INFORMATION:12, **HISTORY:60**}`（体积上限 `_MAX_OPCO_BYTES` 仍兜底）；`gd_evidence_snippets` 同步 |
| 2 | **分层误判**：沿革章一刀切归「补充层（噪音）」，但沿革章同时是**重组叙事唯一出处** | 新增 **`_GROUP_ENTITY_RE` + `_entity_context_hits()`** 与字段 **`gd_opco_entity_count`**；`_gd_tier` 新增 **`核心(沿革章·实体内述)`** |
| 3 | 正则假阳性：裸词 `\bGroup\b` 命中「Guangzhou Finance Holding **Group** Co., Ltd.」（**单测当场抓出**） | 收紧为 `\bour\s+Group\b` |
| 4 | `_gd_tier(r) == "核心"` **精确匹配**把 `核心(沿革章·实体内述)` 从两份清单里整层漏掉 | 改 `.startswith("核心")` |

**全量重扫结果**：离岸 59 家中广东线索命中 **27 → 38 家**；分层 = 核心 23 + 核心(沿革章·实体内述) 2 + 补充 13；
广东并集 **106 → 124 家**（核心口径 111）。Exegenesis 词频 **5 → 35**，从"最弱一档"变为核心层。
已同步重生成：口径 v3、字段说明（27 字段）、结论、BD 台账、三视图、两份证据卡。
**口径文档升级为 `筛选条件_广东红筹.md` v3**（新增 §7 取数方法与根因修正）。

### 三、index 极简化（按用户要求）
`render_site_index` 重写：首页**只有一张卡片**「① 企业红筹架构分析报告」；
**腾讯控股会前简报置于首行并打 `<span class="tag c">样例</span>`**；
全量摸底 9 份文档不再在首页展开，仅在**页脚**留一句批次目录入口（`2026-09-14/index.html` 仍列全部）。

### 四、发布
commit **301f565**（`a742931..301f565` → gh-pages，13 files）；worktree 方式推送后已清理 + `fetch` 同步本地引用。
**线上核验全 200**：首页（1 卡片 1 表 3 行、腾讯首行带样例）、Exegenesis 页（"法人级 / 广州嘉因 / 2025-11-11 /
principal PRC operating entity" 全部命中，旧的"团队级·非法人"表述已清除）、批次页、两张样例页。
**单测 123 全绿**（`test_cover_domicile.py` 扩到 28 例，新增页数预算 + 实体语境 5 例）。

---

## 原开发者回审：新模型完成情况体检（WorkBuddy 扫描全项目）

**结论：完成度高，核心目标达成，且查出并修正了原阶段的系统性 bug。唯一硬伤=新代码 0 提交。**

- **纠偏到位**：`gd_flag` 降为结果表一列，明确「名称是否含粤字不作为条件」；全量摸底 474 家（不做地域过滤）。
- **查出原阶段真 bug**：旧 `extract_structure` 扫整份 PDF 250 页按「离岸>境内>香港」取优先级 → 系统性高估红筹（实测 `id=108344 深圳小阔` 封面明写 PRC 却被判开曼），**翻案 19 条**（全部离岸→境内）。改用**封面页权威口径** `hkex_cover.scan_cover` 修正。
- **关键数字**：全量 474（active 379+listed 95）；开曼 59/境内 396/香港 2/未判定 17；离岸 59 → 广东线索 38（核心 25+补充 13）；名称通道 86；并集 124；VIE 是4/历史19/待核11；测试 123 绿。
- **产出齐全**：`data/listing_full_state.json`+`listing_full.csv`（474×27）、5 视图、口径 v3、字段说明、BD 台账、2 份证据卡、2 份企业报告（錢大媽/Exegenesis）、站点 gh-pages（301f565 已上线）。

**🔴 问题（按严重度）**：
1. **P0：26 项新增/修改全未提交 main**（main HEAD 仍 `a4cf9d8`；`hkex_cover.py`+9 个脚本+数据+测试均 untracked），仅站点在 gh-pages → 工作区丢失=全丢。**须立即 commit**。
2. **P1 旧文档残留**：根目录 `gd_hk_ipo_2026_bd_ledger.md`（v1）与 `output/` 新口径版同名并存；`output/全量摸底_结论.md`（v2，22 家）已被 v3（38 家）取代。
3. **P2 小瑕疵**：`full_listing_conclusion.md` 第五节 38 行表核心/补充层混排无 tier 列；翻案表 #7=#19 重复（广东真健康）。
4. **P2 口径缺口（已披露）**：17 家注册地未判定；396 家境内注册只走名称通道，未做正文运营实体抽取（H 股通道偏窄）。
5. **P2**：`README.md` 未更新（0 处提及全量流水线）。

体检报告：`output/新模型完成情况体检_2026-09-14.md`。

## 提交落地（用户授权"执行"）

P0 已解除：**本地 main 已提交**（未推远程，推送按红线需用户另行明确授权）。
- `59e65ff` feat: 港交所全量摸底流水线 + 封面页权威注册地口径（19 文件 / 5002 行：hkex_cover.py、hkex_listing.py、cli.listing 子命令、11 个 scripts、test_cover_domicile.py+test_guangdong.py 扩展；全量 124 单测绿）
- `726a5e2` chore: 忽略运行日志 *.log（补 .gitignore）
- **刻意排除**：根目录 `gd_hk_ipo_2026_bd_ledger.md`(v1 陈旧台账)、`feasibility_hk_ipo_2026_gd_redchip.md`(早期可行性稿)、`*.log` 运行日志 → 未进历史，待清理。
- data/、output/、*.pdf 按 .gitignore 不入库（生成物）；站点已在 gh-pages。
- 余下未跟踪：仅 2 个陈旧根目录 md，建议删除避免版本混淆。
